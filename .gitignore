# Build output
/build/
/out/
*.exe
*.pdb
*.ilk
*.obj
*.lib
*.exp
# VS / CMake
.vs/
CMakeCache.txt
CMakeFiles/
*.user
# Local-only reference material (manual excerpts, cloned repos, planning notes,
# retired v1 code) — never publish this folder.
/reference/
# ImGui runtime layout (we use a code-defined layout)
imgui.ini
